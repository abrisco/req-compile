# buildifier: disable=bzl-visibility
load("//req_compile/private:compiler.bzl", "py_reqs_solution_test")

py_reqs_solution_test(
    name = "sdist_requirements_test",
    custom_compile_command = "python3 -m req_compile --multiline --hashes --urls --solution $(rootpath //req_compile/private:sdist_requirements.txt) $(rootpath //req_compile/private:sdist_requirements.in)",
    requirements_in = "//req_compile/private:sdist_requirements.in",
    requirements_txt = "//req_compile/private:sdist_requirements.txt",
)
